# Intentionally outdated dependencies — this is a Dependabot demo repo.
# Versions are pinned to old releases so Dependabot has real work to do
# on the first scheduled run. Some of these versions have published CVEs,
# which will also exercise the security-updates path (separate from the
# weekly version-update PRs).

requests==2.34.2
urllib3==1.26.20
pyyaml==5.4.1
python-dotenv==0.21.1
click==8.4.1
boto3==1.43.29
